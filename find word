#include <iostream>
#include <vector>
#include <string>

using namespace std;

bool mencari kata(vector<string>&matriks string data){
int baris = matriks.size();
int kolom = matriks[0].size();
for (int x = 0; x < baris; ++x){
if (matriks[x].find(kata) = string npos){
return false;
}
}
